<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="tip">
	<select id="selectTipList" parameterType="tvo" resultType="tvo">
		select tnum, category, title, contents, createdate, ownerid, ownername
		from tips
	</select>

	<insert id="insertTip" parameterType="tvo">
		insert into tips(category, title, contents, createdate, ownerid, ownername) 
		values(#{category}, #{title}, #{contents}, #{createdate}, #{ownerid}, #{ownername})
	</insert>
</mapper>